import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  imageView: {
    alignItems: "center",
    backgroundColor: "#FFF",
    padding: 8,
  },
  image: {
    width: 120,
    height: 120,
    borderWidth: 3,
    borderColor: "grey",
    borderRadius: 60,
    alignSelf: "center",
  },
});
